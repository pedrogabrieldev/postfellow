import { FormContainer, TextInput, TextArea, Buttons } from './'

export const NewPostForm = () => {
  return (
    <FormContainer>
      <TextInput />
      <TextArea />
      <Buttons />
    </FormContainer>
  )
}
